interface:
  display_name: "Dex Robotics Engineering"
  short_description: "Route retargeting, IK, gain and HIL engineering work"
  default_prompt: "Use $dex-robotics-engineering to select the primary domain workflow and complete this robotics request within its stated scope, reporting the evidence actually established."
policy:
  allow_implicit_invocation: true
