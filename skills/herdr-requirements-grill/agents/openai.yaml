interface:
  display_name: "Herdr Requirements Grill"
  short_description: "Close high-impact Herdr requirement gaps"
  default_prompt: "Use $herdr-requirements-grill to close only the blocking requirement decisions before planning."

policy:
  allow_implicit_invocation: true
