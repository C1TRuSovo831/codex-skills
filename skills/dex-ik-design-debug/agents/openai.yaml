interface:
  display_name: "Dex IK Design Debug"
  short_description: "Design and debug constrained teleoperation IK"
  default_prompt: "Use $dex-ik-design-debug to resolve this IK or virtual-wall problem, preserving continuity and constraints and reporting the verified evidence boundary."
policy:
  allow_implicit_invocation: true
